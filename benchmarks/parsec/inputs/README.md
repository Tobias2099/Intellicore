# PARSEC Inputs

Place local PARSEC input sets here. Input directories are ignored by Git
because they can be large and may have redistribution constraints.

Expected first smoke-test path:

```text
benchmarks/parsec/inputs/simsmall/blackscholes/in_4K.txt
```

