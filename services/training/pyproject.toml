[project]
name = "intellicore-training"
version = "0.1.0"
description = "Training utilities for IntelliCore MARL cache agents."
requires-python = ">=3.11"
dependencies = [
  "pydantic>=2.7",
  "pyyaml>=6.0",
  "numpy>=1.26",
]

[project.scripts]
intellicore-train = "intellicore_training.cli:app"
